// Layout include types

export interface Randomusers {
  id: number;
  name: string;
  image: string;
}

export interface MenuItemsTypes {
  iconString: string;
  colorString: string;
  sizeString: string;
}

export interface MenuItemFollowComTypes {
  user: Randomusers;
}
